# Map feedback: September 8

Ed's Telegram messages require motion immediately after gift selection. His screenshot shows iPhone Safari, a static initial map and a pause icon; his voice note says a first-time visitor also encountered it. The exact device cause is not proven. The former metadata-only preload waited for canplay before requesting playback; the new video declares muted inline autoplay and requests media data immediately. defaultMuted is also set on the element.

Latest user instructions supersede the pause UI and static reduced-motion map: scenario motion runs automatically, including with reduced decorative motion enabled. No pause button or visible explanatory paragraph remains. A start button exists only when the browser rejects playback; browser policy cannot be bypassed. Media is still stopped on navigation/hidden document and resumed when visible.

Callouts retain 18px numerals and 44px targets, sit outside conservative truck clearance zones, and connect to tracked anchors. All 1152 frames are checked for clearance and target separation at short-phone and desktop sizes.

Static city continuation fills the carrier scene around the unchanged moving coordinate plane and behind the choice panel. The generated outpaint was registered against each existing city image before restoring exact centre pixels. No truck motion, outcome or PPE assets changed.

Validation: 17 unit tests, 171 browser tests passed / 29 skipped; static ZIP verifier passed. Inspected 390x844 and 1440x900 screenshots. Native iPhone verification is outstanding: local WebKit installation cannot launch because host ICU/XML/flite libraries are absent. Do not claim Ed's device is verified.
